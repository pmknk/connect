import { colors } from '../colors';

export const MuiInputBase = {
    styleOverrides: {
        root: {
            borderRadius: '0.8rem',
            borderColor: colors.gray[200],
        },
    },
}; 