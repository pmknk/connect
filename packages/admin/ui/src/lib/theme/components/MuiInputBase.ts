import { colors } from '../colors';

export const MuiInputBase = {
    styleOverrides: {
        root: {
            fontSize: '14px',
            height: '46px',
            borderRadius: '0.8rem',
            borderColor: colors.gray[200],
        },
    },
}; 