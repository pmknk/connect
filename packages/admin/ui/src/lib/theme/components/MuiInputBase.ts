import { colors } from '../colors';

export const MuiInputBase = {
    styleOverrides: {
        root: {
            fontSize: '14px',
            height: '46px',
            borderColor: colors.gray[200],
        },
    },
}; 