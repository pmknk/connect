import { colors } from "../colors";

export const MuiIconButton = {
    styleOverrides: {
        root: {
            borderRadius: '0.6rem',
            color: colors.slate[600],
        },
    },
} as const